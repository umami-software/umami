export { createTracker } from './tracker';

export type {
  CustomEventFunction,
  EventData,
  EventProperties,
  PageViewProperties,
  TrackedProperties,
  TrackerConfig,
  UmamiTracker,
  WithRequired,
} from './types';
