import { enUS } from 'date-fns/locale';
import enMessages from 'lang-compiled/en-US.json';

export const messages = {
	'en-US': enMessages
};

export const dateLocales = {
	'en-US': enUS
};

export const menuOptions = [ { label: 'English', value: 'en-US', display: 'en' } ];
